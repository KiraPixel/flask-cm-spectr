apiVersion: v1
kind: Service
metadata:
  name: flask-cm-spectr-service
  namespace: prd-cm
spec:
  selector:
    app: flask-cm-spectr
  ports:
    - port: 31500
    - protocol: TCP
      port: 31500
      targetPort: 31500
  type: NodePort
